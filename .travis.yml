
language: go

go:
  - 1.16.x

git:
  depth: 1

before_install:
  - docker version
  - docker-compose version

before_script:
  - docker-compose up -d --build gogs

script:
  - ./wait-for-gogs.sh
  - make test-in-docker
